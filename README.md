rails3.2.x_google_map
=====================

This is the sample app of google map with rails3.2.x
clone the repository.
Follow the steps:
1. bundle install
2. rake db:create
3. rake db:migrate
then run the command 'rails s'
